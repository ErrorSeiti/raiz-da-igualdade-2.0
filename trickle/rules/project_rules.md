When developing Raiz da Igualdade:
- Maintain the specific Purple/Violet color palette (HSL 270 hue based) representing Dignity and Justice.
- Keep the content structure: Home, Pilares, Mapa Mental, Busca (Map), Equipe.
- Use Portuguese as the primary language for the UI.
- Ensure the layout is responsive for mobile devices.
- Use OpenStreetMap (Leaflet) for map visualizations instead of Google Maps to avoid API key dependency.